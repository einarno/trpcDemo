/* getBooks: publicProcedure
    .query(() => {
        return getBooks();
    }),
    getBooksUrl: publicProcedure
        .input(z.string())
        .query(({ input }) => {
            return {
                url: getBookUrl(input),
            };
        }), 
        */
